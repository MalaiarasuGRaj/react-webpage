{
  "name": "react-webpage",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "PORT=${PORT:-3000} BROWSER=none vite",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port ${PORT:-3000}",
    "test": "echo \"No tests configured for Vite scaffold\" && exit 0"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.8"
  }
}
